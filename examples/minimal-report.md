# Example output

Run the CLI against any local repository:

```bash
npx proofrepo . --format markdown --output PROOFREPO.md
```

The generated report separates passing evidence, warnings, failures, and a claim boundary so readers can distinguish implemented proof from certification claims.
